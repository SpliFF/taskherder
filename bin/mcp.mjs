#!/usr/bin/env node
// taskherd-mcp — MCP server exposing tasks_* tools. Lands in M4 (see DESIGN.md §16).
console.error('taskherd-mcp: not yet implemented (M4 pending). See DESIGN.md.');
process.exit(1);
