#!/usr/bin/env node
// taskherd — CLI entry. Implementation lands in M1 (see DESIGN.md §18, §21).
console.error('taskherd: core CLI not yet implemented (M1 pending). See DESIGN.md.');
process.exit(1);
